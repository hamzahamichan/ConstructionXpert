CREATE TABLE projet (
                         projetId SERIAL PRIMARY KEY,
                         name VARCHAR(255) ,
                         date_debut VARCHAR(255),
                         dateDebut DATE,
                         date_fin DATE,
                         budget DOUBLE
);
